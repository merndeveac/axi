# Pump.fun IDLs

Put verified Pump.fun IDL JSON here if and when a verified IDL is available.

Do not add unverified IDLs without provenance. The decoder can run without an
IDL and will fall back to conservative log, instruction, account, and balance
delta heuristics.
